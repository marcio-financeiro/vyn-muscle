# Vyn Muscle MVP Semanas 1-2 — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Login funcional + 3 telas (Hoje, Histórico, Coach) com dados reais no Supabase; Coach é UI estática sem chamada IA.

**Architecture:** Vanilla JS ES Modules sem build system. Cada tela em `pages/` importa um module script de `js/`. Toda lógica Supabase vive em `js/services/`. Nenhum hex ou nome de fonte fora de `css/tokens.css`.

**Tech Stack:** HTML5, CSS3 (custom properties via tokens.css), ES Modules, @supabase/supabase-js@2 (CDN ESM), Playwright (E2E)

## Global Constraints

- Supabase URL: `https://kfohwrssrnblilcqpjdc.supabase.co`
- Supabase publishable key: `sb_publishable_XdMFEv2ae8gCKYewTuOI_g_Xkr_PPqn`
- SDK via CDN: `https://esm.sh/@supabase/supabase-js@2`
- Toda cor via custom property de `css/tokens.css` — zero hex literal em `css/app.css` ou inline
- Toda fonte via `--fonte-display`, `--fonte-dados`, `--fonte-leitura`
- `--forja` só em CTA principal e sugestão da IA — nunca decorativo
- Nenhuma lógica de negócio em HTML ou script de tela — só em `js/services/`
- Projeto em `/home/marcio/muscle-vyn/` (não no finzen)
- Servidor local: `python3 -m http.server 8080 --directory /home/marcio/muscle-vyn`

---

## Mapa de arquivos

```
muscle-vyn/
  database/
    2026_06_27_treino_logs.sql    ← Task 1 (CREATE)
  css/
    tokens.css                    ← já existe
    app.css                       ← Task 4 (CREATE)
  js/
    config.js                     ← Task 2 (CREATE)
    supabase.js                   ← Task 2 (CREATE)
    services/
      authService.js              ← Task 2 (CREATE)
      treinoService.js            ← Task 3 (CREATE)
    login.js                      ← Task 5 (CREATE)
    hoje.js                       ← Task 6 (CREATE)
    historico.js                  ← Task 7 (CREATE)
    coach.js                      ← Task 8 (CREATE)
  pages/
    login.html                    ← Task 5 (CREATE)
    hoje.html                     ← Task 6 (CREATE)
    historico.html                ← Task 7 (CREATE)
    coach.html                    ← Task 8 (CREATE)
  index.html                      ← Task 9 (CREATE)
  tests/
    mvp.spec.js                   ← Task 10 (CREATE)
```

---

### Task 1: Banco de dados — tabela treino_logs

**Files:**
- Create: `database/2026_06_27_treino_logs.sql`

**Interfaces:**
- Produces: tabela `treino_logs` com RLS ativa, role `authenticated` com GRANT explícito

- [ ] **Step 1: Criar arquivo de migration**

Conteúdo de `database/2026_06_27_treino_logs.sql`:

```sql
create table if not exists treino_logs (
  id         uuid primary key default gen_random_uuid(),
  user_id    uuid references auth.users not null,
  data       date not null default current_date,
  exercicio  text not null,
  series     int,
  reps       int,
  carga      numeric,
  rpe        int check (rpe between 1 and 10),
  obs        text,
  created_at timestamptz not null default now()
);

alter table treino_logs enable row level security;

create policy "select proprio" on treino_logs
  for select to authenticated
  using ( (select auth.uid()) = user_id );

create policy "insert proprio" on treino_logs
  for insert to authenticated
  with check ( (select auth.uid()) = user_id );

create policy "update proprio" on treino_logs
  for update to authenticated
  using  ( (select auth.uid()) = user_id )
  with check ( (select auth.uid()) = user_id );

create policy "delete proprio" on treino_logs
  for delete to authenticated
  using ( (select auth.uid()) = user_id );

grant select, insert, update, delete on treino_logs to authenticated;
```

- [ ] **Step 2: Aplicar no Supabase via MCP execute_sql**

Executar o SQL acima no projeto `kfohwrssrnblilcqpjdc`.

- [ ] **Step 3: Verificar tabela e RLS**

```sql
-- verifica colunas
select column_name, data_type
from information_schema.columns
where table_name = 'treino_logs'
order by ordinal_position;

-- verifica RLS ativa
select relname, relrowsecurity
from pg_class
where relname = 'treino_logs';
```

Esperado: 10 colunas listadas; `relrowsecurity = true`.

- [ ] **Step 4: Commit**

```bash
cd /home/marcio/muscle-vyn
git add database/2026_06_27_treino_logs.sql
git commit -m "feat(db): cria treino_logs com RLS"
```

---

### Task 2: Config + cliente Supabase + authService

**Files:**
- Create: `js/config.js`
- Create: `js/supabase.js`
- Create: `js/services/authService.js`

**Interfaces:**
- Produces:
  - `authService.signIn(email, password)` → `Promise<{ data, error }>`
  - `authService.signUp(email, password)` → `Promise<{ data, error }>`
  - `authService.signOut()` → `Promise<void>`
  - `authService.getSession()` → `Promise<Session | null>`

- [ ] **Step 1: Criar js/config.js**

```js
// js/config.js
export const SUPABASE_URL = 'https://kfohwrssrnblilcqpjdc.supabase.co';
export const SUPABASE_KEY = 'sb_publishable_XdMFEv2ae8gCKYewTuOI_g_Xkr_PPqn';
```

- [ ] **Step 2: Criar js/supabase.js**

```js
// js/supabase.js
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
import { SUPABASE_URL, SUPABASE_KEY } from './config.js';

export const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
```

- [ ] **Step 3: Criar js/services/authService.js**

```js
// js/services/authService.js
import { supabase } from '../supabase.js';

export async function signIn(email, password) {
  return supabase.auth.signInWithPassword({ email, password });
}

export async function signUp(email, password) {
  return supabase.auth.signUp({ email, password });
}

export async function signOut() {
  await supabase.auth.signOut();
}

export async function getSession() {
  const { data } = await supabase.auth.getSession();
  return data.session;
}
```

- [ ] **Step 4: Commit**

```bash
git add js/config.js js/supabase.js js/services/authService.js
git commit -m "feat: config Supabase + authService"
```

---

### Task 3: treinoService

**Files:**
- Create: `js/services/treinoService.js`

**Interfaces:**
- Consumes: `supabase` de `js/supabase.js`, `getSession` de `authService.js`
- Produces:
  - `logSerie({ exercicio, series, reps, carga, rpe, obs })` → `Promise<{ data: TreinoLog, error }>`
  - `getHoje()` → `Promise<{ data: TreinoLog[], error }>`
  - `getTodosLogs()` → `Promise<{ data: TreinoLog[], error }>`

```ts
// TreinoLog (referência de tipo):
// { id, user_id, data, exercicio, series, reps, carga, rpe, obs, created_at }
```

- [ ] **Step 1: Criar js/services/treinoService.js**

```js
// js/services/treinoService.js
import { supabase } from '../supabase.js';
import { getSession } from './authService.js';

export async function logSerie({ exercicio, series, reps, carga, rpe, obs }) {
  const session = await getSession();
  return supabase
    .from('treino_logs')
    .insert({
      user_id:  session.user.id,
      exercicio,
      series:   series  || null,
      reps:     reps    || null,
      carga:    carga   || null,
      rpe:      rpe     || null,
      obs:      obs     || null,
    })
    .select()
    .single();
}

export async function getHoje() {
  const hoje = new Date().toISOString().slice(0, 10); // YYYY-MM-DD
  return supabase
    .from('treino_logs')
    .select('*')
    .eq('data', hoje)
    .order('created_at', { ascending: false });
}

export async function getTodosLogs() {
  return supabase
    .from('treino_logs')
    .select('*')
    .order('created_at', { ascending: false });
}
```

- [ ] **Step 2: Commit**

```bash
git add js/services/treinoService.js
git commit -m "feat: treinoService (logSerie, getHoje, getTodosLogs)"
```

---

### Task 4: css/app.css

**Files:**
- Create: `css/app.css`

**Interfaces:**
- Produces: classes `.topbar`, `.marca-vyn`, `.marca-muscle`, `.nav-abas`, `.nav-aba`, `.conteudo`, `.linha-log`, `.grupo-data`, `.anotacao-ia`, `.form-serie`, `.form-grid`, `.form-group`, `.form-label`, `.input`, `.btn-cta`, `.btn-secondary`, `.btn-adicionar`, `.btn-sair`, `.vazio`, `.login-wrap`, `.login-card`, `.coach-placeholder`

- [ ] **Step 1: Criar css/app.css**

```css
/* css/app.css — estilos das telas. Zero hex aqui: tudo via tokens.css */
@import url('./tokens.css');

*, *::before, *::after { box-sizing: border-box; }

.page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

/* ── Topbar ── */
.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 18px;
  border-bottom: 1px solid var(--linha-forte);
  background: var(--concreto);
}

.marca { display: flex; flex-direction: column; line-height: 1; }

.marca-vyn {
  font-family: var(--fonte-display);
  font-size: 22px;
  color: var(--forja);
  letter-spacing: 0.5px;
  position: relative;
  display: inline-block;
}

.marca-vyn::after {
  content: '';
  position: absolute;
  left: -2px; bottom: -3px;
  width: 110%; height: 2px;
  background: var(--forja);
  transform: rotate(-1.5deg);
}

.marca-muscle {
  font-family: var(--fonte-dados);
  font-size: 9px;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--po);
  margin-top: 5px;
}

.btn-sair {
  font-family: var(--fonte-dados);
  font-size: 11px;
  color: var(--po);
  background: none;
  border: none;
  cursor: pointer;
  letter-spacing: 0.5px;
  padding: 4px 0;
}
.btn-sair:hover { color: var(--cal); }

/* ── Nav abas ── */
.nav-abas {
  display: flex;
  border-bottom: 1px solid var(--linha-forte);
  background: var(--concreto);
}

.nav-aba {
  flex: 1;
  padding: 10px 4px;
  font-family: var(--fonte-dados);
  font-size: 12px;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--po);
  text-align: center;
  text-decoration: none;
  border-bottom: 2px solid transparent;
  transition: color 0.15s, border-color 0.15s;
}
.nav-aba.ativa, .nav-aba:hover {
  color: var(--cal);
  border-bottom-color: var(--forja);
}

/* ── Conteúdo ── */
.conteudo {
  flex: 1;
  padding: 18px;
  max-width: 600px;
  width: 100%;
  margin: 0 auto;
}

/* ── Linha de log ── */
.linha-log {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  padding: 10px 0;
  border-bottom: 1px solid var(--linha-forte);
  font-family: var(--fonte-dados);
  font-size: 13px;
  gap: 8px;
}
.linha-log:last-child { border-bottom: none; }
.linha-log .ex    { color: var(--cal); flex: 1; }
.linha-log .carga { color: var(--cal); font-weight: 600; white-space: nowrap; }
.linha-log .rpe   { color: var(--po); font-size: 11px; white-space: nowrap; }
.linha-log .rpe.alto { color: var(--brasa); }

/* ── Grupo de data (histórico) ── */
.grupo-data {
  font-family: var(--fonte-dados);
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--po);
  padding: 14px 0 6px;
  border-bottom: 1px solid var(--grafite);
}

/* ── Anotação da IA ── */
.anotacao-ia {
  background: var(--forja-fraca);
  border-left: 3px solid var(--forja);
  padding: 12px 14px;
  transform: rotate(-0.4deg);
  font-size: 13.5px;
  line-height: 1.55;
  margin-top: 14px;
  color: var(--cal);
}
.anotacao-ia .tag {
  font-family: var(--fonte-dados);
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--forja);
  display: block;
  margin-bottom: 5px;
}

/* ── Formulário de série ── */
.form-serie {
  background: var(--concreto);
  border: 1px solid var(--linha-forte);
  padding: 16px;
  margin-bottom: 16px;
  display: none;
}
.form-serie.visivel { display: block; }

.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  margin-bottom: 10px;
}
.form-grid .span2 { grid-column: span 2; }

.form-group { display: flex; flex-direction: column; gap: 4px; }

.form-label {
  font-family: var(--fonte-dados);
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--po);
}

.input {
  background: var(--ferro);
  border: 1px solid var(--grafite);
  color: var(--cal);
  font-family: var(--fonte-dados);
  font-size: 13px;
  padding: 8px 10px;
  outline: none;
  width: 100%;
}
.input:focus { border-color: var(--forja); }

/* ── Botões ── */
.btn-cta {
  background: var(--forja);
  color: var(--cal);
  border: none;
  font-family: var(--fonte-dados);
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  padding: 10px 16px;
  cursor: pointer;
  width: 100%;
}
.btn-cta:hover { opacity: 0.9; }
.btn-cta:disabled { opacity: 0.5; cursor: not-allowed; }

.btn-secondary {
  background: none;
  color: var(--po);
  border: 1px solid var(--grafite);
  font-family: var(--fonte-dados);
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 1px;
  padding: 8px 14px;
  cursor: pointer;
}
.btn-secondary:hover { color: var(--cal); border-color: var(--po); }

.btn-adicionar {
  font-family: var(--fonte-dados);
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--forja);
  background: none;
  border: 1px solid var(--forja);
  padding: 8px 14px;
  cursor: pointer;
  margin-bottom: 16px;
}
.btn-adicionar:hover { background: var(--forja-fraca); }

/* ── Estado vazio ── */
.vazio {
  font-family: var(--fonte-dados);
  font-size: 13px;
  color: var(--po);
  padding: 24px 0;
  text-align: center;
}

/* ── Mensagem de erro ── */
.msg-erro {
  font-family: var(--fonte-dados);
  font-size: 12px;
  color: var(--brasa);
  min-height: 16px;
  margin: 0;
}

/* ── Login ── */
.login-wrap {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
}

.login-card {
  background: var(--concreto);
  border: 1px solid var(--linha-forte);
  padding: 32px 28px;
  width: 100%;
  max-width: 360px;
}

.login-marca { text-align: center; margin-bottom: 28px; }

.login-vyn {
  font-family: var(--fonte-display);
  font-size: 36px;
  color: var(--forja);
  display: block;
  line-height: 1;
}

.login-muscle {
  font-family: var(--fonte-dados);
  font-size: 11px;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: var(--po);
  display: block;
  margin-top: 4px;
}

.login-title {
  font-family: var(--fonte-dados);
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--po);
  margin: 0 0 20px;
}

.login-form { display: flex; flex-direction: column; gap: 12px; }

.link-modo {
  font-family: var(--fonte-dados);
  font-size: 12px;
  color: var(--po);
  text-align: center;
  margin-top: 12px;
  cursor: default;
}
.link-modo span { color: var(--cal); text-decoration: underline; cursor: pointer; }

/* ── Coach placeholder ── */
.coach-placeholder {
  font-family: var(--fonte-leitura);
  font-size: 14px;
  color: var(--po);
  line-height: 1.6;
  padding: 24px 0;
  text-align: center;
  margin-top: 32px;
}
```

- [ ] **Step 2: Commit**

```bash
git add css/app.css
git commit -m "feat(css): app.css — estilos das telas"
```

---

### Task 4.5: Desativar confirmação de e-mail no Supabase Auth

**Contexto:** Por padrão, o Supabase exige que o usuário confirme o e-mail antes de poder fazer login. Para o MVP e para os testes Playwright funcionarem, essa confirmação deve ser desativada.

- [ ] **Step 1: Desativar no Dashboard**

No Supabase Dashboard do projeto `vyn-muscle`:
`Authentication → Providers → Email → desativar "Confirm email" → Save`

Sem isso, `signUp` cria o usuário mas `signInWithPassword` falha com "Email not confirmed".

---

### Task 5: pages/login.html + js/login.js

**Files:**
- Create: `pages/login.html`
- Create: `js/login.js`

**Interfaces:**
- Consumes: `authService.signIn`, `authService.signUp`, `authService.getSession`
- Produces: sessão Supabase → redirect `pages/hoje.html`

- [ ] **Step 1: Criar js/login.js**

```js
// js/login.js
import { signIn, signUp, getSession } from './services/authService.js';

async function init() {
  const session = await getSession();
  if (session) { location.replace('./hoje.html'); return; }
  setupForm();
}

function setupForm() {
  let modo = 'login';

  const form     = document.getElementById('form-login');
  const btnSubmit = document.getElementById('btn-submit');
  const inputEmail = document.getElementById('email');
  const inputSenha = document.getElementById('senha');
  const msgErro   = document.getElementById('msg-erro');
  const labelTitulo = document.getElementById('label-titulo');
  const linkModo  = document.getElementById('link-modo');

  linkModo.addEventListener('click', () => {
    modo = modo === 'login' ? 'cadastro' : 'login';
    const isCadastro = modo === 'cadastro';
    labelTitulo.textContent = isCadastro ? 'Criar conta' : 'Entrar';
    btnSubmit.textContent   = isCadastro ? 'Criar conta' : 'Entrar';
    linkModo.innerHTML = isCadastro
      ? 'Já tem conta? <span>Entrar</span>'
      : 'Não tem conta? <span>Criar conta</span>';
    msgErro.textContent = '';
  });

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    msgErro.textContent = '';
    btnSubmit.disabled = true;
    btnSubmit.textContent = 'Aguarde...';

    const fn = modo === 'login' ? signIn : signUp;
    const { error } = await fn(
      inputEmail.value.trim(),
      inputSenha.value
    );

    if (error) {
      msgErro.textContent = error.message;
      btnSubmit.disabled = false;
      btnSubmit.textContent = modo === 'login' ? 'Entrar' : 'Criar conta';
      return;
    }

    location.replace('./hoje.html');
  });
}

init();
```

- [ ] **Step 2: Criar pages/login.html**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vyn Muscle — Login</title>
  <link rel="stylesheet" href="../css/app.css">
</head>
<body class="papel-log">
  <div class="login-wrap">
    <div class="login-card">
      <div class="login-marca">
        <span class="login-vyn">VYN</span>
        <span class="login-muscle">MUSCLE</span>
      </div>
      <p class="login-title" id="label-titulo">Entrar</p>
      <form class="login-form" id="form-login">
        <div class="form-group">
          <label class="form-label" for="email">E-mail</label>
          <input class="input" type="email" id="email" required autocomplete="email">
        </div>
        <div class="form-group">
          <label class="form-label" for="senha">Senha</label>
          <input class="input" type="password" id="senha" required minlength="6" autocomplete="current-password">
        </div>
        <p class="msg-erro" id="msg-erro"></p>
        <button class="btn-cta" type="submit" id="btn-submit">Entrar</button>
      </form>
      <p class="link-modo" id="link-modo">Não tem conta? <span>Criar conta</span></p>
    </div>
  </div>
  <script type="module" src="../js/login.js"></script>
</body>
</html>
```

- [ ] **Step 3: Verificar manualmente**

```bash
python3 -m http.server 8080 --directory /home/marcio/muscle-vyn
```

Abrir `http://localhost:8080/pages/login.html`. Verificar:
- Formulário renderiza com logo VYN / MUSCLE
- Nenhum erro no console do browser
- Link "Criar conta" alterna formulário
- Criar uma conta com e-mail real → redireciona para `hoje.html` (404 ainda — ok)
- Voltar ao login, entrar → redireciona

- [ ] **Step 4: Commit**

```bash
git add pages/login.html js/login.js
git commit -m "feat: login e cadastro (Supabase Auth email/senha)"
```

---

### Task 6: pages/hoje.html + js/hoje.js

**Files:**
- Create: `pages/hoje.html`
- Create: `js/hoje.js`

**Interfaces:**
- Consumes: `treinoService.logSerie`, `treinoService.getHoje`, `authService.getSession`, `authService.signOut`
- Produces: lista do dia atualizada em tempo real após inserção

- [ ] **Step 1: Criar js/hoje.js**

```js
// js/hoje.js
import { getSession, signOut } from './services/authService.js';
import { logSerie, getHoje } from './services/treinoService.js';

function escaparTexto(str) {
  const div = document.createElement('div');
  div.textContent = str;
  return div.innerHTML;
}

function renderLinha(log, prepend = false) {
  const lista = document.getElementById('lista-hoje');
  const vazio = lista.querySelector('.vazio');
  if (vazio) vazio.remove();

  const el = document.createElement('div');
  el.className = 'linha-log';

  const seriesStr = (log.series && log.reps) ? ` — ${log.series}×${log.reps}` : '';
  const cargaStr  = log.carga ? `${log.carga}kg` : '—';
  const rpeClass  = log.rpe >= 9 ? 'rpe alto' : 'rpe';
  const rpeStr    = log.rpe ?? '—';

  const spanEx = document.createElement('span');
  spanEx.className = 'ex';
  spanEx.textContent = log.exercicio + seriesStr;

  const spanCarga = document.createElement('span');
  spanCarga.className = 'carga';
  spanCarga.textContent = cargaStr;

  const spanRpe = document.createElement('span');
  spanRpe.className = rpeClass;
  spanRpe.textContent = `RPE ${rpeStr}`;

  el.append(spanEx, spanCarga, spanRpe);
  if (prepend) lista.prepend(el);
  else lista.appendChild(el);
}

async function carregarHoje() {
  const lista = document.getElementById('lista-hoje');
  const { data, error } = await getHoje();
  if (error || !data?.length) {
    lista.innerHTML = '<p class="vazio">Nenhuma série registrada hoje.</p>';
    return;
  }
  lista.innerHTML = '';
  data.forEach(log => renderLinha(log));
}

async function init() {
  const session = await getSession();
  if (!session) { location.replace('./login.html'); return; }

  document.getElementById('btn-sair').addEventListener('click', async () => {
    await signOut();
    location.replace('./login.html');
  });

  const btnAdicionar = document.getElementById('btn-adicionar');
  const formSerie    = document.getElementById('form-serie');
  const btnCancelar  = document.getElementById('btn-cancelar');

  btnAdicionar.addEventListener('click', () => {
    formSerie.classList.add('visivel');
    btnAdicionar.style.display = 'none';
  });

  btnCancelar.addEventListener('click', () => {
    formSerie.classList.remove('visivel');
    btnAdicionar.style.display = '';
    formSerie.reset();
    document.getElementById('msg-erro-form').textContent = '';
  });

  formSerie.addEventListener('submit', async (e) => {
    e.preventDefault();
    const msgErro  = document.getElementById('msg-erro-form');
    const btnSalvar = document.getElementById('btn-salvar');
    msgErro.textContent = '';
    btnSalvar.disabled  = true;

    const { data, error } = await logSerie({
      exercicio: document.getElementById('exercicio').value.trim(),
      series: parseInt(document.getElementById('series').value)  || null,
      reps:   parseInt(document.getElementById('reps').value)    || null,
      carga:  parseFloat(document.getElementById('carga').value) || null,
      rpe:    parseInt(document.getElementById('rpe').value)     || null,
      obs:    document.getElementById('obs').value.trim()        || null,
    });

    btnSalvar.disabled = false;

    if (error) {
      msgErro.textContent = 'Não foi possível salvar a série. Tente novamente.';
      return;
    }

    renderLinha(data, true);
    formSerie.classList.remove('visivel');
    btnAdicionar.style.display = '';
    formSerie.reset();
  });

  await carregarHoje();
}

init();
```

- [ ] **Step 2: Criar pages/hoje.html**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vyn Muscle — Hoje</title>
  <link rel="stylesheet" href="../css/app.css">
</head>
<body class="papel-log page">

  <header class="topbar">
    <div class="marca">
      <span class="marca-vyn">VYN</span>
      <span class="marca-muscle">MUSCLE</span>
    </div>
    <button class="btn-sair" id="btn-sair">Sair</button>
  </header>

  <nav class="nav-abas">
    <a class="nav-aba ativa" href="./hoje.html">Hoje</a>
    <a class="nav-aba" href="./historico.html">Histórico</a>
    <a class="nav-aba" href="./coach.html">Coach</a>
  </nav>

  <main class="conteudo">
    <button class="btn-adicionar" id="btn-adicionar">+ Série</button>

    <form class="form-serie" id="form-serie">
      <div class="form-grid">
        <div class="form-group span2">
          <label class="form-label" for="exercicio">Exercício</label>
          <input class="input" type="text" id="exercicio" placeholder="ex: Supino reto" required>
        </div>
        <div class="form-group">
          <label class="form-label" for="series">Séries</label>
          <input class="input" type="number" id="series" min="1" max="20" placeholder="4">
        </div>
        <div class="form-group">
          <label class="form-label" for="reps">Reps</label>
          <input class="input" type="number" id="reps" min="1" max="100" placeholder="8">
        </div>
        <div class="form-group">
          <label class="form-label" for="carga">Carga (kg)</label>
          <input class="input" type="number" id="carga" step="0.5" min="0" placeholder="80">
        </div>
        <div class="form-group">
          <label class="form-label" for="rpe">RPE (1–10)</label>
          <input class="input" type="number" id="rpe" min="1" max="10" placeholder="7">
        </div>
        <div class="form-group span2">
          <label class="form-label" for="obs">Obs</label>
          <input class="input" type="text" id="obs" placeholder="ex: última série falhou">
        </div>
      </div>
      <p class="msg-erro" id="msg-erro-form"></p>
      <div style="display:flex;gap:8px;">
        <button class="btn-cta" type="submit" id="btn-salvar" style="flex:1">Salvar</button>
        <button class="btn-secondary" type="button" id="btn-cancelar">Cancelar</button>
      </div>
    </form>

    <div id="lista-hoje">
      <p class="vazio">Carregando...</p>
    </div>
  </main>

  <script type="module" src="../js/hoje.js"></script>
</body>
</html>
```

- [ ] **Step 3: Testar manualmente**

Com servidor em `http://localhost:8080`:
- Acessar `hoje.html` sem sessão → redireciona para `login.html`
- Logar → `hoje.html` carrega, estado vazio "Nenhuma série registrada hoje."
- Clicar "+ Série", preencher, salvar → linha aparece no topo da lista sem reload
- Verificar no Supabase Dashboard (`Table Editor > treino_logs`) que o registro foi inserido

- [ ] **Step 4: Commit**

```bash
git add pages/hoje.html js/hoje.js
git commit -m "feat: tela Hoje — registrar série + lista do dia"
```

---

### Task 7: pages/historico.html + js/historico.js

**Files:**
- Create: `pages/historico.html`
- Create: `js/historico.js`

**Interfaces:**
- Consumes: `treinoService.getTodosLogs`, `authService.getSession`, `authService.signOut`
- Produces: todos os logs agrupados por data, decrescente

- [ ] **Step 1: Criar js/historico.js**

```js
// js/historico.js
import { getSession, signOut } from './services/authService.js';
import { getTodosLogs } from './services/treinoService.js';

function formatarData(isoDate) {
  const [, mes, dia] = isoDate.split('-');
  return `${dia}/${mes}`;
}

function renderHistorico(data) {
  const lista = document.getElementById('lista-historico');

  if (!data?.length) {
    lista.innerHTML = '<p class="vazio">Nenhum treino registrado ainda. Registre a primeira série em Hoje.</p>';
    return;
  }

  // Agrupar por data
  const grupos = {};
  data.forEach(log => {
    if (!grupos[log.data]) grupos[log.data] = [];
    grupos[log.data].push(log);
  });

  lista.innerHTML = '';
  Object.keys(grupos)
    .sort((a, b) => b.localeCompare(a))
    .forEach(dataStr => {
      const cabecalho = document.createElement('div');
      cabecalho.className = 'grupo-data';
      cabecalho.textContent = formatarData(dataStr);
      lista.appendChild(cabecalho);

      grupos[dataStr].forEach(log => {
        const el = document.createElement('div');
        el.className = 'linha-log';

        const seriesStr = (log.series && log.reps) ? ` — ${log.series}×${log.reps}` : '';
        const cargaStr  = log.carga ? `${log.carga}kg` : '—';
        const rpeClass  = log.rpe >= 9 ? 'rpe alto' : 'rpe';

        const spanEx = document.createElement('span');
        spanEx.className = 'ex';
        spanEx.textContent = log.exercicio + seriesStr;

        const spanCarga = document.createElement('span');
        spanCarga.className = 'carga';
        spanCarga.textContent = cargaStr;

        const spanRpe = document.createElement('span');
        spanRpe.className = rpeClass;
        spanRpe.textContent = `RPE ${log.rpe ?? '—'}`;

        el.append(spanEx, spanCarga, spanRpe);
        lista.appendChild(el);
      });
    });
}

async function init() {
  const session = await getSession();
  if (!session) { location.replace('./login.html'); return; }

  document.getElementById('btn-sair').addEventListener('click', async () => {
    await signOut();
    location.replace('./login.html');
  });

  const { data, error } = await getTodosLogs();
  if (error) {
    document.getElementById('lista-historico').innerHTML =
      '<p class="vazio">Erro ao carregar histórico.</p>';
    return;
  }
  renderHistorico(data);
}

init();
```

- [ ] **Step 2: Criar pages/historico.html**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vyn Muscle — Histórico</title>
  <link rel="stylesheet" href="../css/app.css">
</head>
<body class="papel-log page">

  <header class="topbar">
    <div class="marca">
      <span class="marca-vyn">VYN</span>
      <span class="marca-muscle">MUSCLE</span>
    </div>
    <button class="btn-sair" id="btn-sair">Sair</button>
  </header>

  <nav class="nav-abas">
    <a class="nav-aba" href="./hoje.html">Hoje</a>
    <a class="nav-aba ativa" href="./historico.html">Histórico</a>
    <a class="nav-aba" href="./coach.html">Coach</a>
  </nav>

  <main class="conteudo">
    <div id="lista-historico">
      <p class="vazio">Carregando...</p>
    </div>
  </main>

  <script type="module" src="../js/historico.js"></script>
</body>
</html>
```

- [ ] **Step 3: Testar manualmente**

- `historico.html` sem sessão → redireciona para `login.html`
- Logado → exibe registros agrupados por data, mais recente primeiro
- RPE ≥ 9 em `var(--brasa)` (tom avermelhado)
- Estado vazio: "Nenhum treino registrado ainda..."

- [ ] **Step 4: Commit**

```bash
git add pages/historico.html js/historico.js
git commit -m "feat: tela Histórico — logs agrupados por data"
```

---

### Task 8: pages/coach.html + js/coach.js

**Files:**
- Create: `pages/coach.html`
- Create: `js/coach.js`

**Interfaces:**
- Consumes: `authService.getSession`, `authService.signOut`
- Produces: UI estática do mockup do brand book, sem chamada IA (Semana 3)

- [ ] **Step 1: Criar js/coach.js**

```js
// js/coach.js
import { getSession, signOut } from './services/authService.js';

async function init() {
  const session = await getSession();
  if (!session) { location.replace('./login.html'); return; }

  document.getElementById('btn-sair').addEventListener('click', async () => {
    await signOut();
    location.replace('./login.html');
  });
}

init();
```

- [ ] **Step 2: Criar pages/coach.html**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vyn Muscle — Coach</title>
  <link rel="stylesheet" href="../css/app.css">
</head>
<body class="papel-log page">

  <header class="topbar">
    <div class="marca">
      <span class="marca-vyn">VYN</span>
      <span class="marca-muscle">MUSCLE</span>
    </div>
    <button class="btn-sair" id="btn-sair">Sair</button>
  </header>

  <nav class="nav-abas">
    <a class="nav-aba" href="./hoje.html">Hoje</a>
    <a class="nav-aba" href="./historico.html">Histórico</a>
    <a class="nav-aba ativa" href="./coach.html">Coach</a>
  </nav>

  <main class="conteudo">
    <div class="linha-log">
      <span class="ex">Agachamento — últimas 3 sessões</span>
      <span class="carga">↑ carga</span>
      <span class="rpe">↑ RPE</span>
    </div>
    <div class="anotacao-ia">
      <span class="tag">Anotação da IA</span>
      Carga subindo, mas RPE também. Próxima sessão: mantenha 120kg e teste 5×5 em vez de subir peso.
    </div>
    <p class="coach-placeholder">
      O Coach analisa seu histórico e sugere ajustes.<br>Disponível em breve.
    </p>
  </main>

  <script type="module" src="../js/coach.js"></script>
</body>
</html>
```

- [ ] **Step 3: Commit**

```bash
git add pages/coach.html js/coach.js
git commit -m "feat: tela Coach — UI estática do mockup"
```

---

### Task 9: index.html (redirect)

**Files:**
- Create: `index.html`

- [ ] **Step 1: Criar index.html**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Vyn Muscle</title>
  <script type="module">
    import { getSession } from './js/services/authService.js';
    const session = await getSession();
    location.replace(session ? './pages/hoje.html' : './pages/login.html');
  </script>
</head>
<body></body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add index.html
git commit -m "feat: index.html com redirect auth-aware"
```

---

### Task 10: Testes Playwright

**Files:**
- Create: `tests/mvp.spec.js`
- Modificar: `package.json` (criar se não existir)

**Pré-requisito:** invocar skill `playwright-cli` para configurar o ambiente de testes.

- [ ] **Step 1: Invocar skill playwright-cli**

Invocar `playwright-cli` antes de escrever qualquer teste para setup do ambiente.

- [ ] **Step 2: Iniciar servidor local em background**

```bash
python3 -m http.server 8080 --directory /home/marcio/muscle-vyn &
```

- [ ] **Step 3: Criar tests/mvp.spec.js**

```js
// tests/mvp.spec.js
import { test, expect } from '@playwright/test';

const BASE  = 'http://localhost:8080';
const EMAIL = `teste.mvp+${Date.now()}@vynmuscle.test`;
const SENHA = 'senhateste123';

async function logar(page, email, senha) {
  await page.goto(`${BASE}/pages/login.html`);
  await page.fill('#email', email);
  await page.fill('#senha', senha);
  await page.click('#btn-submit');
  await page.waitForURL(/hoje/, { timeout: 15000 });
}

test('login page renderiza sem erro de console', async ({ page }) => {
  const errors = [];
  page.on('console', m => { if (m.type() === 'error') errors.push(m.text()); });
  await page.goto(`${BASE}/pages/login.html`);
  await expect(page.locator('.login-vyn')).toBeVisible();
  await expect(page.locator('#email')).toBeVisible();
  expect(errors.filter(e => !e.includes('favicon'))).toHaveLength(0);
});

test('hoje.html sem sessão redireciona para login', async ({ page }) => {
  await page.goto(`${BASE}/pages/hoje.html`);
  await page.waitForURL(/login/, { timeout: 8000 });
  await expect(page).toHaveURL(/login/);
});

test('historico.html sem sessão redireciona para login', async ({ page }) => {
  await page.goto(`${BASE}/pages/historico.html`);
  await page.waitForURL(/login/, { timeout: 8000 });
  await expect(page).toHaveURL(/login/);
});

test('coach.html sem sessão redireciona para login', async ({ page }) => {
  await page.goto(`${BASE}/pages/coach.html`);
  await page.waitForURL(/login/, { timeout: 8000 });
  await expect(page).toHaveURL(/login/);
});

test('criar conta + logar + redirecionar para hoje', async ({ page }) => {
  await page.goto(`${BASE}/pages/login.html`);
  // Alternar para modo "Criar conta"
  await page.locator('#link-modo span').click();
  await expect(page.locator('#label-titulo')).toHaveText('Criar conta');
  await page.fill('#email', EMAIL);
  await page.fill('#senha', SENHA);
  await page.click('#btn-submit');
  await page.waitForURL(/hoje/, { timeout: 15000 });
  await expect(page).toHaveURL(/hoje/);
});

test('hoje.html — registrar série aparece na lista', async ({ page }) => {
  await logar(page, EMAIL, SENHA);
  await page.click('#btn-adicionar');
  await expect(page.locator('.form-serie')).toBeVisible();
  await page.fill('#exercicio', 'Supino reto');
  await page.fill('#series', '4');
  await page.fill('#reps', '8');
  await page.fill('#carga', '80');
  await page.fill('#rpe', '7');
  await page.click('#btn-salvar');
  const linha = page.locator('.linha-log').first();
  await expect(linha).toBeVisible({ timeout: 8000 });
  await expect(linha.locator('.ex')).toContainText('Supino reto');
  await expect(linha.locator('.carga')).toHaveText('80kg');
});

test('historico.html — exibe logs do usuário', async ({ page }) => {
  await logar(page, EMAIL, SENHA);
  await page.goto(`${BASE}/pages/historico.html`);
  await expect(page.locator('.linha-log').first()).toBeVisible({ timeout: 8000 });
  await expect(page.locator('.grupo-data').first()).toBeVisible();
});

test('coach.html — carrega UI estática sem erro de console', async ({ page }) => {
  const errors = [];
  page.on('console', m => { if (m.type() === 'error') errors.push(m.text()); });
  await logar(page, EMAIL, SENHA);
  await page.goto(`${BASE}/pages/coach.html`);
  await expect(page.locator('.anotacao-ia')).toBeVisible();
  await expect(page.locator('.coach-placeholder')).toBeVisible();
  expect(errors.filter(e => !e.includes('favicon'))).toHaveLength(0);
});
```

- [ ] **Step 4: Executar os testes**

```bash
cd /home/marcio/muscle-vyn
npx playwright test tests/mvp.spec.js --reporter=line
```

Esperado: 8 testes passando.

- [ ] **Step 5: Commit final + push**

```bash
git add tests/ package.json playwright.config.js
git commit -m "test: Playwright E2E para as 3 telas do MVP"
git push
```

Aguardar deploy Vercel e verificar `https://<projeto>.vercel.app`.
